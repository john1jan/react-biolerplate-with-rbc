
import * as VALUES from "./values";
import * as utils from "./utils";

export function logPageView() {
    if ((typeof window !== "undefined")) {
        //Log Page views
    }
}

export function track(category, action, properties) {
    // Log events herer
}   







